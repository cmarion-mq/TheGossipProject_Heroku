<div class="container pt-5">
    <h1>Author <%= params[:id] %></h1>
    <p class="mt-3"></p>
    <div class="card mt-3">
        <div class="card-body">
            <h5 class="card-title"><%=User.find(params[:id]).first_name%>  <%=User.find(params[:id]).last_name%></h5>
            <div class="card-text"><%=User.find(params[:id]).age%> ans, habite à <a href="<%= city_path(User.find(params[:id]).city_id) %>"> <%=City.find(User.find(params[:id]).city_id).name%></div></a>
            <div class="card-text"><%= User.find(params[:id]).email %></div>
            <p><p/>
            <div class="card-text">Desription : <%= User.find(params[:id]).description %></div>
            <div class="mt-3"
            </div>
        </div>
    </div>
</div>