<div class="container pt-5">
    <h1>Potin <%= params[:id] %></h1>
    <p class="mt-3"></p>
    <div class="card mt-3">
        <div class="card-body">
            <h5 class="card-title"><%= User.find(params[:id]).first_name  %></h5>
            <div class="card-text"><%= Gossip.find(params[:id]).content %></div>
            <div class="card-text">crée le <%= Gossip.find(params[:id]).created_at.strftime("%d/%m/%Y") %></div>
            <div class="mt-3"
            <span class="nav-link"> <%= link_to Gossip.find(params[:id]).user.first_name, "/author/#{Gossip.find(params[:id]).user.id}", class: "btn btn-secondary" %></span>
            </div>
        </div>
    </div>
</div>